<%= form_with(model: video_chat) do |form| %>
  <% if video_chat.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(video_chat.errors.count, "error") %> prohibited this video_chat from being saved:</h2>

      <ul>
        <% video_chat.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="text-white">
    <%= form.label :title, style: "display: block" %>
    <%= form.text_area :title %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
